#include<stdio.h>
#include<iostream>
#include<bits/stdc++.h>
using namespace std;


int main() {
    string str;
    int i, j;
    vector<string> operations;
    while (cin>>str) {
        if (str[0]=="Q") {
            ptr = strtok(str, " ");
            i = ptr[1];
            j = ptr[2];
            for (auto it = operations.rbegin(); it != operations.rend(); ++it)
            {
                if ()
            }
        }
    }


    return 0;
}